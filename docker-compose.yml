version: "3"

services:

  redis:
    image: redis:alpine
    ports:
      - 6379

  mafia-bot:
    depends_on:
      - redis
    build: .
    environment:
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
      - REDIS_HOST=redis
